# 🏦 Financial Radar — Платформа обнаружения подозрительных операций

## 💡 Описание
Сервис для анализа финансовых транзакций в реальном времени с поддержкой:
- REST API приёма транзакций
- Асинхронной обработки через in-memory очередь
- Четырёх типов правил: Threshold, Pattern, Composite, ML
- Админ-панели для управления правилами и просмотра транзакций
- Уведомлений (Email, Webhook)
- Структурированного логирования с `correlationId`

Система реализует **модульный монолит** с чёткими границами ответственности, соответствует требованиям хакатона и готова к демонстрации «из коробки».

---

## 🚀 Запуск

```bash
docker-compose up --build
```

После запуска сервис будет доступен по адресам:
- **API**: `http://localhost:8000/api/transactions`
- **Админ-панель**: `http://localhost:8000/admin`

> 💡 Первый запуск может занять 1–2 минуты (сборка образа, инициализация БД).

---

## 📦 Стек технологий

- **Backend**: Python 3.11 + FastAPI
- **База данных**: PostgreSQL
- **ML**: Scikit-learn / XGBoost (предобученная модель)
- **Шаблонизация**: Jinja2 (SSR)
- **Контейнеризация**: Docker + Docker Compose

---

## 🧪 Демо-сценарий

### 1. Создание правила
1. Перейдите в `/admin/rules`
2. Создайте правило:
   - **ID**: `high_amount`
   - **Тип**: `threshold`
   - **Params**:
     ```json
     {"field": "amount", "op": ">", "value": 100000}
     ```
   - **Включено**: ✅

### 2. Отправка транзакции
```bash
curl -X POST http://localhost:8000/api/transactions \
  -H "Content-Type: application/json" \
  -d '{
    "timestamp": "2025-10-26T12:00:00Z",
    "sender_account": "ACC123456",
    "receiver_account": "ACC987654",
    "amount": 150000,
    "transaction_type": "transfer",
    "location": "Moscow",
    "device_used": "mobile",
    "payment_channel": "card",
    "ip_address": "192.168.1.100",
    "device_hash": "D1234567"
  }'
```

### 3. Результат
- Статус в `/admin`: **⚠️ ALERT**
- Причина: `amount > 100000`
- Correlation ID: `...`
- Уведомление: приходит на webhook.site (если настроен)

---

## 🗃️ Структура проекта

```
financial-radar/
├── app/                  # Основной код сервиса
│   ├── main.py           # Точка входа
│   ├── models/           # DTO и валидация
│   ├── api/              # REST-контроллеры
│   ├── queue/            # In-memory очередь
│   ├── rules/            # Движок правил (4 типа)
│   ├── storage/          # Работа с PostgreSQL
│   ├── notify/           # Уведомления (Email/Webhook)
│   ├── admin/            # Админ-панель (SSR)
│   └── observability/    # Структурированные логи
├── resources/
│   └── model.pkl         # Предобученная ML-модель
├── templates/            # HTML-шаблоны админки
├── static/               # CSS/JS
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
└── README.md
```

---

## 🔐 Безопасность

- Валидация всех входных полей (amount > 0, формат аккаунтов, ISO-8601 timestamp)
- Экранирование данных в админ-панели (защита от XSS)
- Basic Auth для админки (настраивается через `.env`)
- Конфигурация через переменные окружения (без хардкода секретов)

---

## 📊 Наблюдаемость

- **Структурированные логи** в формате JSON:
  ```json
  {"correlationId": "...", "component": "rules", "ruleId": "high_amount", "message": "Rule triggered"}
  ```
- **Метрики**:
  - processed / alerted / reviewed
  - latency (p50/p95)
  - очередь: размер, время ожидания
- Все события одной транзакции связаны через `correlationId`

---

## 🧩 Поддерживаемые типы правил

| Тип | Пример | Хранение |
|-----|--------|----------|
| **Threshold** | `amount > 100000` | `{"field":"amount","op":">","value":100000}` |
| **Pattern** | 3 транзакции ≤500 за 10 мин | `{"by":"sender_account","windowMin":10,"maxAmount":500,"minCount":3}` |
| **Composite** | `(amount > 50000) AND (hour in [23,0,1,2,3,4,5])` | `{"expr": "amount > 50000 and hour in [23, 0, 1, 2, 3, 4, 5]"}` |
| **ML-Rule** | Вероятность мошенничества ≥ 0.7 | `{"threshold": 0.7}` |

Все правила:
- Хранятся в таблице `rules` (PostgreSQL)
- Загружаются при старте
- Обновляются **без перезапуска** через админ-панель
- Имеют приоритет и версию

---

## 📤 Экспорт и отчёты

- В админ-панели: кнопка **«Экспорт в CSV»** для текущего списка транзакций
- Агрегированная статистика: processed / alerted / reviewed

---

## 🛠 Требования к окружению

- Docker Engine ≥ 20.10
- Docker Compose ≥ 2.0
- Свободное место: ≥ 500 МБ
- Память: ≥ 1 ГБ

---
